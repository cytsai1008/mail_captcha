{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "lint": "npm run lint:js && npm run lint:css && npm run lint:html",
    "lint:js": "eslint .",
    "lint:css": "stylelint \"**/*.css\"",
    "lint:html": "htmlhint \"**/*.html\"",
    "start": "http-server dist -c-1",
    "build": "mkdir -p dist && cp *.html *.css *.js theme.js -r dist"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "htmlhint": "^1.7.1",
    "http-server": "^14.1.1",
    "stylelint": "^16.24.0",
    "stylelint-config-standard": "^39.0.0"
  }
}
